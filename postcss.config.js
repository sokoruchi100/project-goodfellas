module.exports = {
  plugins: [
    require("tailwindcss"), // Include the Tailwind CSS plugin
    require("autoprefixer"), // Include the Autoprefixer plugin to add vendor prefixes
    // You can add more PostCSS plugins here if needed
  ],
};
